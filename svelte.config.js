// Minimal Svelte config. Without this, @astrojs/svelte logs "No Svelte config
// found" on every dev start. We don't need preprocessing (TS is handled via
// lang="ts" in each component) but having the file eliminates the warning.

export default {};
